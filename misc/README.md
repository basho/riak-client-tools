# Misc

* `setup_tls.bin` - binary file containing full `RpbStartTls` message, ready to send on a socket to Riak
* `setup_auth.bin` - binary file containing full `RpbAuth` message, with user `riakpass` and password `Test1234`
